#[project]
#name = "backend"
#version = "0.1.0"
#description = "Add your description here"
#readme = "README.md"
#requires-python = ">=3.14"
#dependencies = [
 #   "fastapi>=0.141.1",
  #  "google-generativeai>=0.8.6",
   # "httpx2>=2.12.0",
    #"pytest>=9.1.1",
    #"python-dotenv>=1.2.3",
    #"python-multipart>=0.0.32",
    #"uvicorn>=0.52.4",
#]

#[project.scripts]
#backend = "backend:main"

#[build-system]
#requires = ["uv_build>=0.12.9,<0.13.0"]
#build-backend = "uv_build"
#UV was used a at the begining but later migrated to pip
